# Bootstrap: winit + wgpu + vello + taffy + parley

Protótipo de aprendizado (triângulo → UI com flexbox). **Não faz parte do crate `lemon`** — o rust-analyzer ignora esta pasta.

Para referência ou copiar trechos de volta para `src/`.
